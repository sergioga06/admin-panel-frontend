apiVersion: v1
kind: Service
metadata:
  name: admin-panel-frontend-service
spec:
  selector:
    app: admin-panel-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3033
      # Puedes elegir un puerto fijo entre 30000-32767
      nodePort: 32033 
  type: NodePort # <--- CAMBIA ESTO